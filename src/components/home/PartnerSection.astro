---
import type { LocaleContent } from "../../data/site";

interface Props {
  content: LocaleContent;
}

const { content } = Astro.props;
---

<section class="section partner-section">
  <article class="reference-card partner-callout">
    <div class="partner-logo-tile">
      <img
        src="/images/top-clean-logo.png"
        alt={content.partnerCompany.name}
        width="443"
        height="422"
        loading="lazy"
        decoding="async"
      />
    </div>

    <div class="partner-callout-copy">
      <p class="eyebrow">{content.partnerCompany.kicker}</p>
      <h2>{content.partnerCompany.title}</h2>
      <p>{content.partnerCompany.intro}</p>
    </div>

    <a
      class="button button-secondary"
      href={content.partnerCompany.href}
      target="_blank"
      rel="noreferrer"
    >
      {content.partnerCompany.cta}
    </a>
  </article>
</section>
