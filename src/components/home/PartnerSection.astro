---
import type { LocaleContent } from "../../data/site";

interface Props {
  content: LocaleContent;
}

const { content } = Astro.props;
---

<section class="section partner-section">
  <div class="section-heading">
    <p class="eyebrow">{content.partnerCompany.kicker}</p>
    <h2>{content.partnerCompany.title}</h2>
    <p>{content.partnerCompany.intro}</p>
  </div>

  <article class="reference-card partner-card">
    <h3>{content.partnerCompany.name}</h3>
    <a
      class="button button-secondary"
      href={content.partnerCompany.href}
      target="_blank"
      rel="noreferrer"
    >
      {content.partnerCompany.cta}
    </a>
  </article>
</section>

<style>
  .partner-section .section-heading {
    margin-bottom: 1.5rem;
  }

  .partner-card {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    padding: 1.5rem 2rem;
    border-radius: var(--radius-md);
    background: var(--partner-card-bg);
    border: 1px solid var(--partner-section-border);
    overflow: hidden;
  }

  .partner-card h3 {
    margin: 0;
    white-space: nowrap;
  }

  .partner-card .button {
    flex-shrink: 0;
    white-space: nowrap;
    background: linear-gradient(135deg, #0c4f72 0%, #0e6984 52%, #1ea4a0 100%);
    border-color: rgba(12, 79, 114, 0.22);
    color: #f9fdff;
    box-shadow: 0 14px 28px rgba(8, 45, 67, 0.18);
  }

  .partner-card .button:hover,
  .partner-card .button:focus-visible {
    background: linear-gradient(135deg, #0b4768 0%, #0d6179 52%, #1a9895 100%);
    color: #ffffff;
  }

  @media (max-width: 600px) {
    .partner-card {
      flex-direction: column;
      align-items: stretch;
      padding: 1.25rem 1.5rem;
    }

    .partner-card h3,
    .partner-card .button {
      white-space: normal;
    }
  }
</style>
